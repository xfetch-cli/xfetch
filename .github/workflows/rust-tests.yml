name: Rust Tests

on:
  # Local-only CI (ci.sh / ci.ps1). Runs here only when triggered manually.
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test
